snippet  entity
    entity ${1:entity} is
        port(${2});
    end ${1:entity};
    ${0}

snippet architecture
    architecture ${1:rtl} of ${2:entity} is
    ${3}
    begin
        ${4}
    end ${1:rtl};
    ${0}

snippet configuration
    configuration ${1:cfg} of ${2:entity} is
        for ${3:architecture}
        end for;
    end ${1:cfg};
    ${0}

snippet process
    process ${1} begin
        ${2}
    end process;
    ${0}
