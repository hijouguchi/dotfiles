snippet     module
abbr        module with reg
prev_word   '^'
    module ${1:module_name}(clk, xrst${2:, and_others});
    input clk, xrst${3};${4}
    always @(posedge clk or negedge xrst) begin
        if(!xrst)
            ${5}
    end${6}
    endmodule${0}

snippet     function
abbr        function
prev_word   '^'
    function ${1:[n:0] func_name}(${2:input_ports});
        input $2;${3}
    endfunction${0}
